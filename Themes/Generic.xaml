<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:BKKleaner.UI">

    <ui:NullToCollapsedConverter x:Key="Generic.NullToCollapsed"/>

    <Style TargetType="{x:Type ui:BusyOverlay}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:BusyOverlay}">
                    <Border Background="{DynamicResource Brush.Overlay}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Grid Width="48" Height="48" HorizontalAlignment="Center">
                                <Ellipse Width="48" Height="48" StrokeThickness="4"
                                         Stroke="{DynamicResource Brush.Border}" Opacity="0.4"/>
                                <Ellipse x:Name="PART_Spinner" Width="48" Height="48" StrokeThickness="4"
                                         Stroke="{DynamicResource Brush.Accent}"
                                         StrokeDashArray="3.2 5" StrokeDashCap="Round"/>
                            </Grid>
                            <TextBlock Text="{TemplateBinding Message}" Margin="0,16,0,0"
                                       HorizontalAlignment="Center" FontWeight="SemiBold"
                                       Foreground="{DynamicResource Brush.Text}"
                                       Visibility="{Binding Message, RelativeSource={RelativeSource TemplatedParent},
                                                    Converter={StaticResource Generic.NullToCollapsed}}"/>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
